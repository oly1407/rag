server:
  port: 8080

spring:
  datasource:
    # 连接你截图里的 MySQL（把密码换成你自己的）
    url: jdbc:mysql://localhost:3306/aliyun?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: 140722
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  ai:
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      api-key: ${QWEN_API_KEY:sk-65db12a66eba4c4ba552cea736f54378}
      chat:
        options:
          model: qwen-turbo
          temperature: 0.4
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

app:
  qwen:
    app-id: your-qwen-app-id
  chat:
    max-history-messages: 60
